.b-region{class: "#{empty?(region)}"}
  .uk_name
    %span
      = link_to region.uk_name, edit_admin_region_path(region)
  .ru_name
    %span
      = region.uk_name
  .en_name
    %span
      = region.uk_name
