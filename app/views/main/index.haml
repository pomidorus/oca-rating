%section.l-main-index
  = render 'header'
  -#= render 'datasets'

  %section.l-cities
    %header
      %h2
        = @region_header
    .s-cities
      = render 'regions_filter'
      %section.b-cities
        = render partial: 'city', collection: @cities
